.. contents::

Introduction
============

Start an OrionVM virtual machine, waits in the foreground till Ctrl-C then shuts down the vm and exits. As a secondary mode of operation - after start it can run a command such as ssh and shutdown the vm after the command terminates.
